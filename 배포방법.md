# 폰에서 보기 — GitHub Pages 배포

클라우드에서 2시간마다 자동으로 수집·갱신하고, 폰 브라우저로 주소에 접속해 보는 방식입니다.
**PC를 꺼둬도 계속 돌아갑니다.**

```
GitHub Actions (2시간마다)
    → youtube_trending.py 실행
    → docs/index.html 갱신 + snapshots/ 누적
    → GitHub Pages 가 자동 배포
    → 폰에서 https://아이디.github.io/저장소이름/ 접속
```

---

## 1단계 · 저장소 만들기

1. <https://github.com/new> 접속
2. Repository name에 `yt-trending` (아무 이름이나)
3. **Public** 선택
   > 무료 계정은 Public에서만 Pages를 쓸 수 있습니다.
   > API 키는 저장소가 아니라 Secrets에 들어가므로 공개되지 않습니다.
   > 올라가는 것은 공개된 유튜브 조회수 통계뿐입니다.
4. **Create repository**

---

## 2단계 · 파일 올리기

### 방법 A — git 사용 (권장)

git이 없다면 먼저 설치합니다.

```powershell
winget install --id Git.Git -e --source winget
```

**설치 후 PowerShell 창을 완전히 닫고 새로 여세요.** PATH가 갱신되어야 인식됩니다.

```powershell
cd C:\Users\Danny\yt
git --version          # 버전이 나오면 준비 완료
```

> `winget` 자체가 없다는 오류가 나면
> <https://git-scm.com/download/win> 에서 설치 파일을 받아 기본값으로 설치하세요.

이제 업로드합니다.

```powershell
cd C:\Users\Danny\yt
git init
git add .
git commit -m "유튜브 급상승 추적기"
git branch -M main
git remote add origin https://github.com/아이디/yt-trending.git
git push -u origin main
```

`아이디` 부분을 본인 GitHub 아이디로 바꾸세요.
푸시할 때 로그인 창이 뜨면 브라우저로 승인하면 됩니다.
`.gitignore`가 있어 로컬 결과물과 키 파일은 올라가지 않습니다.

### 방법 B — 설치 없이 웹으로

1. 저장소 첫 화면 → **Add file → Upload files**
2. 탐색기에서 아래 파일들을 끌어다 놓고 **Commit changes**
   - `youtube_trending.py`
   - `진단.py`
   - `설정방법.md`
   - `배포방법.md`
   - `.gitignore`
3. 워크플로 파일은 따로 만듭니다 → **Add file → Create new file**
4. 파일명 칸에 정확히 입력

   ```
   .github/workflows/update.yml
   ```

   > 슬래시를 입력하면 폴더가 자동으로 만들어집니다. **이게 핵심입니다.**
   > 윈도우 탐색기에서는 `.` 으로 시작하는 폴더가 숨겨져 있어
   > 끌어다 놓기로는 누락되기 쉽습니다.

5. 내용 칸에 `update.yml` 파일 내용을 통째로 붙여넣고 **Commit changes**

`snapshots` 폴더는 첫 실행 때 자동으로 만들어지므로 올리지 않아도 됩니다.

---

## 3단계 · API 키 등록 ⭐

**여기가 가장 중요합니다. 키를 코드에 절대 넣지 마세요.**

1. 저장소 → **Settings** → 왼쪽 **Secrets and variables** → **Actions**
2. **New repository secret**
3. Name: `YOUTUBE_API_KEY`
4. Secret: 발급받은 키 붙여넣기
5. **Add secret**

이렇게 하면 워크플로 로그에도 `***` 로 가려져 나옵니다.

---

## 4단계 · 첫 실행

**Pages보다 먼저 실행해야 합니다.** Actions가 돌아야 `docs` 폴더가 생기고,
그래야 다음 단계에서 Pages를 켰을 때 첫 접속부터 화면이 제대로 뜹니다.

1. 저장소 → **Actions** 탭
   > 처음 들어가면 워크플로 실행을 허용할지 묻는 초록 버튼이 나올 수 있습니다. 승인하세요.
2. 왼쪽 **유튜브 급상승 갱신** 클릭
3. 오른쪽 **Run workflow** → 다시 **Run workflow**
4. 1~3분 뒤 초록 체크가 뜨면 완료

실행 결과를 클릭해 **수집 및 페이지 생성** 단계를 펼치면
후보 개수·카테고리 분포·숏츠 비율이 그대로 보입니다.
게임 제외가 잘 적용됐는지도 여기서 확인하세요.

---

## 5단계 · Pages 켜기

1. 저장소 → **Settings** → 왼쪽 **Pages**
2. Source: **Deploy from a branch**
3. Branch: `main` / 폴더: **`/docs`** → **Save**

1~2분 뒤 같은 화면 위쪽에 주소가 뜹니다.

```
https://아이디.github.io/yt-trending/
```

이제 폰에서 이 주소로 접속하면 됩니다.

---

## 폰 홈화면에 추가

앱처럼 쓰려면:

- **아이폰(Safari)** — 공유 버튼 → `홈 화면에 추가`
- **안드로이드(Chrome)** — 메뉴(⋮) → `홈 화면에 추가`

주소창 없이 전체화면으로 열리고, 아이콘과 상태바 색도 맞춰뒀습니다.

---

## 갱신 주기 바꾸기

`.github/workflows/update.yml` 의 `cron` 을 고치세요. **UTC 기준**입니다.

```yaml
- cron: "5 */2 * * *"    # 2시간마다 (기본)
- cron: "5 * * * *"      # 1시간마다
- cron: "0 21 * * *"     # 매일 한국시간 오전 6시 (UTC 21시)
```

> GitHub 무료 스케줄은 정시에 딱 맞춰 돌지 않고 몇 분~수십 분 늦을 수 있습니다.
> 급상승 추적 용도에는 문제가 없는 수준입니다.

**할당량 계산** — 1회 실행에 약 150~200 units, 하루 10,000 한도입니다.
1시간마다 돌려도 하루 4,800 정도라 여유롭습니다.

---

## 저장소가 무거워지지 않게

스냅샷은 `publishedAt` 과 `views` 만 저장합니다. 제목·설명은 어차피 매번 다시 받아오므로
보관할 이유가 없습니다. 1,000개 영상 기준 **0.9 MB → 61 KB (15배 절감)** 입니다.

보관 정책도 자동으로 적용됩니다.

| 시점 | 보관 |
|---|---|
| 최근 2일 | 전부 |
| 3일 ~ 35일 | 하루 3개 |
| 35일 초과 | 삭제 |

정상 운영 시 스냅샷 총량은 **약 7 MB** 선에서 유지됩니다.

---

## 문제가 생기면

**Actions가 빨간 X로 실패**
Actions 탭 → 실패한 실행 클릭 → 로그 확인.
`YOUTUBE_API_KEY 시크릿이 없습니다` 가 뜨면 3단계를 다시 하세요.

**페이지가 404**
Pages 설정에서 폴더가 `/docs` 인지 확인하세요. 첫 배포는 몇 분 걸립니다.

**내용이 갱신되지 않음**
저장소가 60일간 활동이 없으면 GitHub이 스케줄을 자동 중지합니다.
Actions 탭에서 **Enable workflow** 를 누르면 다시 시작됩니다.

**할당량 초과**
`update.yml` 의 실행 명령에 `--regions KR` 이나 `--deep 0` 을 추가해 절약하세요.
