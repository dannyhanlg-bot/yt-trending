# 유튜브 급상승 추적 — 사용 설명서

유튜브가 2025년 7월 전 카테고리 급상승 집계를 중단했기 때문에, 이 도구는 순위를 **직접 계산**합니다.

---

## 실행

```powershell
cd C:\Users\Danny\yt
$env:YOUTUBE_API_KEY = "발급받은_키"
python youtube_trending.py
```

키를 매번 넣기 번거로우면 한 번만 `setx YOUTUBE_API_KEY "키"` 를 실행해 두세요.
(적용하려면 창을 새로 열어야 합니다.)

---

## 일간 / 주간 / 월간

세 탭 모두 **"그 기간에 올라온 영상을 누적 조회수 순으로"** 줄 세웁니다.

| 탭 | 대상 | 순위 기준 |
|---|---|---|
| 일간 | 최근 24시간 업로드 | 누적 조회수 |
| 주간 | 최근 7일 업로드 | 누적 조회수 |
| 월간 | 최근 30일 업로드 | 누적 조회수 |

**하루 평균 속도가 아니라 총 조회수 기준입니다.** 이 둘은 순서가 완전히 뒤집힙니다.

| 영상 | 누적 조회수 | 하루 평균 | 누적 순위 | 평균 순위 |
|---|---|---|---|---|
| 25일 전 대형작 | 2,000만 | 80만 | **1위** | 3위 |
| 2일 전 | 500만 | 250만 | 2위 | 2위 |
| 6시간 전 폭발 | 200만 | 800만 | 3위 | **1위** |

카드의 요약문에도 하루 평균이 나오지만 `(참고: …)` 로 표기되며, 순위와는 무관합니다.

### 과거 수집분 병합

후보 풀을 오늘자 인기 차트에서만 만들면, **3주 전에 크게 터졌지만 지금은 차트에서
내려간 영상**이 월간 목록에서 통째로 빠집니다. 그래서 보관된 스냅샷을 뒤져
지난 30일 안에 올라온 영상을 되살리고, 조회수를 최신값으로 갱신해 함께 순위에 넣습니다.

```
· 과거 수집분 복원      412개 (조회수 최신화)
```

돌린 횟수가 쌓일수록 주간·월간이 촘촘해집니다. `--archive 0` 으로 끄면 오늘 차트만 씁니다.
**스냅샷을 지우지 마세요.**

---

## 영상 설명

각 카드에 채널이 작성한 **영상 설명**이 회색 인용 블록으로 들어갑니다.
API 응답에 이미 포함된 값이라 추가 호출 비용은 없습니다.

유튜브 설명란은 대개 링크와 홍보 문구로 뒤덮여 있어 그대로 쓰면 읽을 수 없습니다.
그래서 아래를 걷어내고 첫 대목만 최대 140자로 다듬습니다.

| 제거 대상 | 예 |
|---|---|
| URL | `https://…`, `www.…` |
| 타임스탬프 목차 | `00:00 인트로`, `01:23 1절` |
| 해시태그·멘션 | `#신곡`, `@channel` |
| 구독·SNS 유도 (짧은 줄) | `구독하기`, `인스타그램 팔로우`, `비즈니스 문의` |

정리 후 남는 내용이 없으면 설명 블록은 표시되지 않습니다.

---

## 숏츠 / 롱폼 구분

화면 세 번째 줄의 알약 버튼으로 **전체 · ⚡ 숏츠 · 🎬 롱폼**을 전환합니다.
각 탭은 **독립적으로 1위부터 순위를 다시 매깁니다.** 숏츠 탭의 1위는 숏츠 중 1위입니다.

API에는 "이 영상은 숏츠"라는 필드가 없어서 길이로 판별합니다.

| 길이 | 판정 |
|---|---|
| 60초 이하 | 숏츠 (확실) |
| 61~180초 | **애매** — `youtube.com/shorts/{id}` 가 리다이렉트 없이 열리는지 확인 |
| 180초 초과 | 롱폼 |
| 길이 불명 (라이브 등) | 롱폼 |

2024년부터 숏츠 상한이 3분으로 늘어, 짧은 뮤직비디오·예고편이 이 구간에 섞입니다.
그래서 애매한 구간만 실제 URL로 확인합니다. 조회수 상위 120개까지만 확인하므로
시간이 크게 늘지는 않습니다.

확인 절차를 건너뛰려면:

```powershell
python youtube_trending.py --no-verify-shorts   # 더 빠름, 2~3분 영상이 숏츠로 섞일 수 있음
```

---

## 자주 돌릴수록 정확해집니다 ⭐

이전 기록이 있으면 '오늘 급상승'이 **누적 평균 속도 대신 최근 구간의 실제 속도**를 씁니다.

| 실행 간격 | 잡히는 것 |
|---|---|
| 하루 1회 | 업로드 이후 평균 속도 — 대략적인 급상승 |
| **1~2시간** | **지금 이 순간 가속 중인 영상** ← 권장 |

1회 실행에 약 **150 units**만 쓰고 하루 한도가 10,000이므로,
1시간마다 돌려도 하루 3,600으로 여유롭습니다.

```powershell
# 예: 2시간마다 자동 실행 (작업 스케줄러 등록)
schtasks /create /tn "유튜브급상승" /tr "python C:\Users\Danny\yt\youtube_trending.py" /sc hourly /mo 2
```

---

## 특정 카테고리 빼기

게임 영상이 너무 많다면:

```powershell
python youtube_trending.py --exclude 게임
```

한글·영문·숫자 ID를 모두 인식하며, 쉼표로 여러 개 지정할 수 있습니다.

```powershell
python youtube_trending.py --exclude 게임,스포츠
python youtube_trending.py --exclude 20,17        # 위와 동일
```

제외는 **두 단계**로 이뤄집니다. 해당 카테고리 차트를 아예 요청하지 않고(호출도 절약),
전체 인기 차트나 채널 신작 경로로 섞여 들어온 것도 한 번 더 걸러냅니다.
제외된 상태는 화면 상단과 하단 각주에 표시됩니다.

### 주요 카테고리 ID

| ID | 이름 | ID | 이름 |
|---|---|---|---|
| 1 | 영화·애니 | 22 | 인물·브이로그 |
| 2 | 자동차 | 23 | 코미디 |
| 10 | 음악 | 24 | 엔터테인먼트 |
| 15 | 반려동물 | 25 | 뉴스·정치 |
| 17 | 스포츠 | 26 | 스타일·하우투 |
| 19 | 여행 | 27 | 교육 |
| **20** | **게임** | 28 | 과학기술 |

실행할 때마다 콘솔에 **카테고리 분포**가 찍히니, 어떤 분류가 과대 대표되는지 보고 결정하세요.

```
· 카테고리 분포   음악 120(28%), 엔터테인먼트 95(22%), 게임 88(21%) …
```

> **주의** — 스케줄 등록 시 플래그를 매번 동일하게 유지하세요.
> 어제는 제외하고 오늘은 안 하면 스냅샷 구성이 달라집니다.
> (다만 기준 시점 이전에 올라온 영상은 자동으로 걸러지므로 순위가 크게 왜곡되지는 않습니다.)

---

## 옵션

```powershell
python youtube_trending.py --regions KR       # 한국만
python youtube_trending.py --top 30           # 30개까지 표시
python youtube_trending.py --deep 0           # 채널 신작 탐색 생략 (더 빠름)
python youtube_trending.py --deep 80          # 더 많은 채널의 신작까지 확보
python youtube_trending.py --exclude 게임     # 게임 카테고리 제외
```

---

## 순위가 매겨지는 방식

| 항목 | 내용 |
|---|---|
| 후보 풀 | ① 지역 전체 인기 차트(최대 200) ② 전 카테고리 인기 차트 ③ 인기 채널의 24시간 내 신작 |
| 일간 / 주간 / 월간 | **최근 24시간 / 7일 / 30일** 업로드를 **누적 조회수** 내림차순 |
| 제외 대상 | 조회수 비공개, `--exclude` 로 지정한 카테고리 |
| 영상 설명 | 채널 작성 설명에서 링크·해시태그·홍보 문구를 걷어낸 첫 대목 (최대 140자) |
| 수치 요약 | 수집된 숫자만으로 자동 생성 (내용 추측 없음) |

**글로벌 탭은 미국 차트 기준입니다.** 유튜브는 인기 차트를 지역별로만 집계하므로
'전 세계 통합' 차트라는 것은 존재하지 않습니다.

---

## 문제가 생기면

```powershell
python 진단.py
```

파라미터를 하나씩 더해가며 어디서 막히는지 찍어줍니다.

| 증상 | 원인 |
|---|---|
| `할당량을 모두 사용했습니다` | 태평양시간 자정에 리셋. `--regions KR --deep 0` 으로 절약 가능 |
| `API 키가 유효하지 않습니다` | Cloud Console에서 YouTube Data API v3 **사용 설정** 누락 |
| 후보 0개 | `진단.py` 실행 후 결과 확인 |

---

## 파일 설명

| 파일 | 역할 |
|---|---|
| `youtube_trending.py` | 수집 + 순위 계산 + HTML 생성 |
| `진단.py` | API 호출이 막힐 때 원인 추적 |
| `trending-youtube.html` | 결과 웹페이지 (실행할 때마다 덮어씀) |
| `snapshots/` | 시점별 조회수 기록. **지우지 마세요** — 증가량 계산의 근거입니다 |

---

## 참고 · v1에서 무엇이 바뀌었나

v1은 `search` API로 후보를 모았는데, **검색어 없이 호출하면 결과가 0건**으로 돌아온다는
사실이 진단으로 확인됐습니다. `search`는 목록 API가 아니라 검색 API이기 때문입니다.

v2는 `videos.list?chart=mostPopular` 로 교체했습니다.

| | v1 `search` | v2 `mostPopular` |
|---|---|---|
| 호출 비용 | 100 units | **1 unit** |
| 1회 실행 | 약 4,800 units | **약 150 units** |
| 조회수 | 별도 호출 필요 | 응답에 포함 |
| 실제 동작 | ❌ 0건 | ✅ 정상 |
